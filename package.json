{
  "name": "bugstream",
  "version": "2.0.0",
  "description": "Privacy-first SDK for generating self-contained HTML bug reports with screen recording, console logs, network requests, and keyboard events",
  "type": "module",
  "main": "dist/bugstream-sdk.js",
  "scripts": {
    "dev": "vite build --watch",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "fflate": "^0.8.2",
    "rrweb": "^2.0.0-alpha.17"
  },
  "devDependencies": {
    "terser": "^5.31.0",
    "typescript": "^5.5.3",
    "vite": "^5.4.2"
  }
}
